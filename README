A really basic 'bootloader' (sort of - it doesn't actually load any external code, but it boots for sure and could load with minor additions).
Keepin' it real-mode, homies.

Associated Blog Post: http://www.reinterpretcast.com/creating-a-bare-bones-bootloader
